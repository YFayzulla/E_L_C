{{--
    Legacy layout name kept for the views that still reference it.
    It now simply inherits the single application shell so there is one
    navbar / sidebar / theme implementation to maintain.
--}}
@extends('template.master')
